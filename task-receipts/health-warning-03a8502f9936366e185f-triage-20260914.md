# Health warning triage: expired genome delivery

At 03:06:14Z `mesh-chat-deliver` reported message `f4df48579a778ce0` for genome (window
5964517) failed with `age-expiry`, age limit 900 seconds, and zero delivery attempts. This is a
permanent expiration of that queued message; the original body is not present in the current
board record, so it cannot be faithfully reconstructed or replayed from this evidence.

Current live state at 04:11Z is `genome` IDLE with a clean `Ask Codex to do anything` prompt.
The recent replay shows later successful genome sends at 03:46:55Z and earlier times, but does
not establish the content or delivery of the expired message. No replacement was sent because
the missing body makes an exact retry unsafe. The known blindness is that the expired message's
content and intended action are unavailable in the current delivery record.

Verification: `mesh-mind-state genome`, `mesh-tell --peek genome`, and
`mesh-tell --replay -n 30 genome`; the exact failure is in `~/.mesh/chat.log` at 03:06:14Z.
