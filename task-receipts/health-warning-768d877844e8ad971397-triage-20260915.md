# Health warning triage — `health-warning/768d877844e8ad971397`

Task: `health-warning/768d877844e8ad971397/triage`  
Owner: `health`  
Warning: `mesh-land` autoland overlap refused at `2026-09-15T19:18:01Z` because a prior run
was still active past the next cadence boundary.

## Evidence

- `~/.mesh/land.log` contains the overlap refusal three times at the end of the observed run.
- The same log shows the preceding run processed an autoland batch, held 15 candidates, and
  had a real settled candidate land (`health-warning-8007dac789c004dc9421-triage-20260915.md`).
  It then reports `autoland overlap refused` on subsequent cadence attempts.
- `scripts/mesh-land:58-70` uses the shared non-blocking lock
  `${MESH_DIR:-$HOME/.mesh}/.mesh-land-run.lock`; the refusal is therefore the intended
  concurrency guard, not evidence of a corrupt repository or failed land operation.
- At triage time no `mesh-land`/`mesh-autoland` process was live and the lock was not held.
  The condition is historical/transient rather than a currently wedged run.
- `~/.mesh/mesh-land-backlog.tsv` still has a large classification-pending bulk backlog,
  including many held receipts and scripts. This is known queue pressure, not a substrate fault.

## Disposition

Report-only: the overlap guard worked and the prior run subsequently released. No safe health
action or substrate change is justified. Preserve the overlap evidence; the separate
`health-warning/d1682ad40a18cb0be846/triage` row covers repeated overlap-alert deduplication and
belongs to the mesh-land/autoland owner lane. Do not start a competing land run or edit the lock.

## Verification

- Read the recent `~/.mesh/land.log` overlap and batch records.
- Inspected `scripts/mesh-land` lock wiring at lines 58–70.
- Checked live process and lock state; no active holder remained at triage time.
