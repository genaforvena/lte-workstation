# Health warning triage — peer path, LAN presence, and egress

Task: `health-warning/91e759e44c7618d27790/triage`  
Source: `health@mesh-home` check-stream delta at 2026-09-13T23:57:31Z.

## Findings

The original delta reported imac-rozalia changing from direct to relay, win-q6gl9fir3qi changing
from direct to offline, router presence unknown, and `mesh-doctor` still running after roughly eight
minutes with partial egress findings. At the 2026-09-14 21:14–21:17Z recheck:

- `mesh-path-watch --status` reports 2 direct, 0 relay, and 6 offline peers. imac-rozalia and
  Phaedra are direct. `tailscale status` also shows imac-rozalia direct and Phaedra direct. Thus the
  imac relay event is recovered/intermittent, not a current relay condition.
- The Windows peer is not safely classifiable as healthy or offline from this node: condensed
  `tailscale status` prints no connection state, while `tailscale status --json` reports
  `Online=true`, `Active=false`, an empty current address, and a zero `LastSeen`. Preserve this as
  an observation ambiguity; do not infer reachability from the `Online` field alone.
- `mesh-lan-presence --nodes` produced no result within a 20-second bound (exit 124). Router/LAN
  presence therefore remains UNKNOWN. Retry when router reachability is restored or the probe can
  complete within its bound; no router or route action is justified by this evidence.
- Fresh `~/.mesh/egress-health.log` samples through 21:16:40Z report `OK egress=US`, public IP
  `38.49.216.141`, Anthropic status `405`, `path=route`, and quality `0%` loss / about `134 ms`
  average. The 24-hour baseline has `0/439` bad results and 37 unattributed results. This confirms
  current sampled egress health, while 37 unattributed observations remain a measurement gap.
- A bounded `timeout 90s mesh-doctor --quiet` run emitted the policy-aware NOTE that configured
  exit-node `n2sbt7yy6t11CNTRL` is an intentional consumer dependency (single-node egress SPOF),
  followed by unrelated warnings, then ended at the 90-second bound with exit 124. The completed
  `mesh-doctor-egress-policy-20260914` receipt and task establish that the former role-blind egress
  detector was corrected and tested; this partial live run confirms its consumer classification,
  not the full doctor aggregate.

## Disposition

Classify the imac relay delta as recovered; keep Windows peer reachability and router/LAN presence
as named unknowns; retain 37/439 unattributed quality observations as an egress attribution gap.
The intentional Phaedra exit-node dependency remains visible as a single-node SPOF note. No exact
active prerequisite task was needed: the related role-aware doctor correction is complete, and the
remaining items are external reachability/measurement conditions. No route, DNS, firewall, VPN, or
Tailscale state was changed.

## Verification

- `mesh-dash --once check` at 21:14:29–21:14:38Z: current egress OK; pane still reports router/LAN
  presence unknown and 37 unattributed 24-hour observations.
- `mesh-path-watch --status` at 21:14:01Z: imac-rozalia and Phaedra direct; zero relays.
- `tailscale status` and its JSON output at 21:17Z: Phaedra exit-node online; Windows peer fields
  as described above.
- `mesh-lan-presence --nodes` bounded at 20 seconds: exit 124, no result.
- `mesh-doctor --quiet` bounded at 90 seconds: policy-aware egress NOTE observed; exit 124 before
  aggregate completion.
- Egress log read through 21:16:40Z: latest quality sample OK; 0/439 bad and 37 unattributed.
- `mesh-task check dispatch health-warning/91e759e44c7618d27790/triage health`: exit 0; row taken
  by `health`.

Retry the LAN-presence probe when router reachability returns or its bounded read completes; revisit
the Windows peer when Tailscale supplies a current endpoint/last-seen observation; rerun full doctor
after the automated checks no longer hold it beyond the 90-second window.
