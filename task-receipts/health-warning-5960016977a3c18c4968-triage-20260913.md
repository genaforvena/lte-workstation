# Health warning triage: `health-warning/5960016977a3c18c4968`

Checked 2026-09-13 19:28–19:31 UTC on `mesh-home`.

## Finding

Witness posted message `2275a29550da7e2a` to Genome at 18:49:49Z asking for a
safe path-scoped landing of two receipts after autoland refused a stale stash.
The delivery worker reported age expiry at 19:04:56Z (age 907 seconds against
the 900-second bound). Its durable ledger entry has `attempts=0`,
`terminal_reason=age-expiry`, and `status=failed`.

The delivery code sends only when two pane captures are identical after a
2.5-second delay. It increments `attempts` only when `mesh-tell` exits zero and
discards that command's output. Therefore this record means no successful send
was recorded; it cannot distinguish a pane-gate skip from a failed
`mesh-tell` call. Genome had an active task during this period (board activity
from 19:01:46Z through 19:08:33Z), consistent with a busy recipient, but this
does not prove what the pane gate saw at the exact retry times.

Nearby witness-to-Genome deliveries also expired with zero recorded attempts
at 18:59, 19:15, and 19:20Z. This is a repeated recipient-delivery visibility
signal, not evidence of a routing, DNS, firewall, VPN, or DMS fault. The
operator's mesh-home substrate HOLD is acknowledged; no substrate or delivery
policy changes were made.

## Evidence

- `/home/mesh-home/.mesh/chat.log:60599`: original witness FYI.
- `/home/mesh-home/.mesh/chat-deliver.log:2562`: failure at 19:04:56Z, age
  902s as measured by the worker, attempts 0, target window `5964420`.
- `/home/mesh-home/.mesh/chat-deliver-ledger.json:2542-2552`: first seen
  18:49:49Z, attempts 0, failed at 19:04:56Z, terminal reason age-expiry.
- `scripts/mesh-chat-deliver:43-53,109-124`: stable-pane gate and attempt
  accounting; `mesh-tell` output is discarded.
- `/home/mesh-home/.mesh/chat.log:60645-60668`: Genome task activity across
  the expiry interval.
- The adjacent triage `task-receipts/health-warning-e4d6ec4c655c3d814be6-triage-20260913.md`
  reached the same evidence limit for another Genome-targeted expiry.

## Disposition

Classified as a bounded historical age-expiry with an unresolved failed-hop
cause. No dispatcher fix is justified by these records alone; any diagnostic
change needs its own scoped task. The mesh-home substrate HOLD remains active.
