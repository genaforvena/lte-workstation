# Health-warning triage — 2026-09-15

Task: `health-warning/8346dc50f9528050e920/triage`

## Disposition

This warning is a live chronic suppression roll-up for imac-rozalia SSH
access, but it is an external credential/authentication blind spot rather than
a mesh-home transport outage. Transport is currently healthy; the safe result
is to preserve the known limitation and retry when valid imac SSH credentials
or an owner-authorized independent LAN path becomes available.

## Evidence

- `mesh-dash --once check` at 2026-09-15T14:54:15Z showed mesh-home working,
  supervised egress, and GPU healthy; known BT/mesh alarms and stale peers
  remain.
- `mesh-health` at 2026-09-15T14:52:17Z reported `PASS imac-rozalia`.
- `tailscale status` at 14:52Z showed imac-rozalia active and direct at
  `192.168.8.214:56461`.
- The bounded SSH probe to `100.121.88.110` reached the host and returned
  `Permission denied (publickey,password,keyboard-interactive)`.
- Prior same-signature receipts document repeated direct transport plus SSH
  authentication refusal; no safe substrate action exists from this node.

The warning is unresolved externally, not rejected as duplicate or cleared.
Retry condition: valid SSH credentials or an owner-authorized independent
LAN/SSH path for imac-rozalia.

## Verification

- `mesh-task queue --dispatch --owner health` returned this exact row first.
- `mesh-task check dispatch health-warning/8346dc50f9528050e920/triage health`
  exited 0.
- `MESH_TASK_ACTOR=health mesh-task take health-warning/8346dc50f9528050e920 triage`
  succeeded.

No code or substrate state changed.
