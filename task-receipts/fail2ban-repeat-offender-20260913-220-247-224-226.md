# Fail2ban repeat-offender triage

- Task: `fail2ban-repeat-offender-20260913-220-247-224-226/triage-repeat-offender`
- Owner: `health`
- Observed: `2026-09-16T00:28Z`
- Alert source: `~/.mesh/chat.log:58556`, dated `2026-09-13T00:20:42Z`
- Alert: `220.247.224.226` received three SSH bans in six hours across ban/expiry cycles.

## Evidence checked

- `~/.mesh/fail2ban-watch.log`: absent on this node (`stat`: ENOENT).
- `fail2ban-client status sshd`: unavailable because `fail2ban-client` is not installed/on PATH.
- `/var/log/auth.log` and `/var/log/auth.log.1`: no matching `220.247.224.226` lines.
- The current local auth log was fresh at `2026-09-16T00:28:35Z` and was searched through the current file plus one rotation.

## Verdict

The historical alert is corroborated by the mesh chat record, but current offender activity,
ban state, and present access risk cannot be established from this node. This is a known
visibility blind, not evidence that the offender is inactive or safe.

Retry condition: obtain the watch log and the fail2ban/sshd state from `phaedra`, the alerting
node, then repeat the triage. No firewall, jail, routing, or other substrate state was changed.
