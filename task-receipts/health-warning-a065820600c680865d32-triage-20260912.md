# Health warning triage: check-stream roll-call

Checked 2026-09-12 11:00–11:08 UTC for live task `health-warning/a065820600c680865d32/triage`.

## Result

The route and LAN visibility gaps remain. Fresh evidence confirms this node's default egress still
uses the Tailscale overlay and the selected exit node remains `phaedra`, so the single-exit-node
dependency remains. LAN presence remains UNKNOWN from this node. This is a reporting-only triage;
no substrate state was changed.

The requested comprehensive doctor rerun did not complete. It reproduced two FAILs and several
WARNs before entering tool smoke tests, then stopped producing output. The doctor shell was blocked
reading a pipe with no remaining child process; I interrupted this diagnostic attempt. No fresh
comprehensive total is claimed.

## Evidence

- `mesh-dash --once check` at 11:00:57 UTC reported the doctor cache from 09:32:30 UTC (3 FAIL / 34
  WARN, stale), egress on `tailscale0`, exit node configured, high load, and UVC metadata DARK.
- `ip route get 1.1.1.1` at 11:07 UTC returned `dev tailscale0 table 52 src 100.81.222.19`.
- `tailscale status --json` at 11:07 UTC showed configured exit node `phaedra` (`n2sbt7yy6t11CNTRL`)
  online and marked as the active exit node.
- `mesh-lan-presence --nodes` at 11:08 UTC exited 1: router unreachable; no local address in
  `192.168.8.0/24`, ARP cannot see that segment, and no known host answered ICMP. Verdict: UNKNOWN.
- `mesh-doctor --comprehensive` began at 11:01 UTC and was interrupted at 11:07 UTC after entering
  tool smoke tests. It reproduced FAILs for egress over `tailscale0` and the configured exit-node
  SPOF, with Anthropic reachable. Prior output included WARNs for the default mic device, untimed
  peer SSH in `mesh-load-audit`, six `mesh-song-verify` bypasses of the `librosa-analysis` funnel,
  and five absence-as-negative sites. The smoke-test phase and totals are incomplete.

## Disposition

Keep the egress/exit-node and LAN observability gaps open as known health gaps. Do not use the cached
doctor count as current. A complete fresh comprehensive total remains unresolved because this scan
stalled during smoke tests. No routing, DNS, VPN, or firewall state was changed.
