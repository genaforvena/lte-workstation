# Health warning triage: transient NVMe thermal episode

Task: `health-warning/4e97700c330b40cbc3a4/triage`

The reported 2026-09-10 12:19Z state was real: `storage-health.log` records
`DEGRADED — thermal=HOT (leading indicator — latency not yet degraded)` with capacity NORMAL, latency
FAST, and media HEALTHY. Its next transition at 12:34Z was HEALTHY with thermal WARM, FAST latency,
and media HEALTHY. The episode cleared after about 15 minutes without storage or substrate changes.

The readings recur intermittently, so the underlying thermal episodes are not permanently gone:
`nvme-temp.log` records HOT readings on 2026-09-11 at 09:45Z and 11:00Z and on 2026-09-12 at
10:30Z. Current independent reads at 13:31Z show recovery:

- `mesh-storage-health`: HEALTHY — capacity NORMAL, latency FAST, thermal WARM, media HEALTHY.
- `mesh-nvme-temp`: WARM, Sensor 1 at 66.8°C; composite 39.9°C; maximum threshold 74.85°C and
  critical threshold 79.85°C.
- `mesh-disk-health`: `nvme0n1` await 0.064 ms, queue depth 0, 4.5% utilization; `nvme1n1` idle.
- `mesh-nvme-gradient`: BALANCED, 1.0°C delta across the two controllers.

The storage-health log itself last changed on 2026-09-11 at 11:19Z, but it is transition-gated.
The scheduled `mesh-storage-health --edge` run is wired at minutes 04/19/34/49, and cron journal
records the 13:19Z run. Thus the old log mtime is not a liveness failure; the current manual fusion
and scheduled-run evidence agree that the present state is healthy.

Verdict: close this historical episode as recovered. Recurrent HOT readings remain a known thermal
warning, but current storage latency and media health are normal and present temperature is below
the tool's HOT boundary. No cooling, storage, or substrate setting was changed. Continue observing
the scheduled temperature and storage-health transitions; escalate if HOT persists or latency/media
health degrades.

Verification: inspected the exact historical HOT→HEALTHY transition records, current temp/gradient/
disk/storage-health output, crontab wiring, and the 13:19Z cron journal entry.
