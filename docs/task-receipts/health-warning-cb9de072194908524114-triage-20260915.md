# TinyFleet S06 resource-admission receipt — 2026-09-15

Task: `health-warning/cb9de072194908524114/triage` (owner `health`).

## Live read

At `2026-09-15T21:09:57Z`, a non-destructive `nvidia-smi` read reported:

```text
GPU 0 NVIDIA GeForce RTX 3060: total=12288 MiB used=9593 MiB free=2320 MiB util=0% temp=45C
```

The registered S06 launch minimum is `2048 MiB` (`scripts/mesh-study-launch`,
`MESH_STUDY_GPU_MIN_FREE_MB`, default 2048). The observed headroom is therefore
`2320 - 2048 = 272 MiB`; the threshold is met, but this is not a comfortable
admission margin and must not be described as idle GPU capacity.

## Shared consumers

The same live sample listed these CUDA consumers:

```text
PID 1520     /home/mesh-home/.venv-ai/bin/python  800 MiB
PID 2038     /home/mesh-home/.venv-ai/bin/python  2160 MiB
PID 2856965  /usr/local/lib/ollama/llama-server  3806 MiB
PID 3776683  /usr/local/lib/ollama/llama-server  2802 MiB
```

`ollama ps` simultaneously showed `qwen3-vl:4b-instruct` and
`gemma4:e2b-it-qat`, both `100% GPU`. The user services
`mesh-voice-clone.service`, `mesh-room-gigaam.service`, and `ollama.service`
were all active. No service or workload was stopped, restarted, or preempted.

## Verdict and exact predicate

This is **threshold-met but fragile**, not “safe headroom”: free VRAM is
nonzero and above 2 GiB, while concurrent managed consumers leave only 272 MiB
of slack. The receipt does not authorize starting S06 from this observation.

For a retry/admission decision, take a fresh successful query
`nvidia-smi --query-gpu=memory.free --format=csv,noheader,nounits` immediately
before the attempt and parse one numeric MiB value. Then:

```text
RETRY/DEFER iff the query fails, is non-numeric/missing, or free_mib < 2048.
ADMIT iff the query succeeds with numeric free_mib >= 2048.
```

Because the registered predicate has no larger safety reserve, an admission at
`2048..2319 MiB` is formally eligible but operationally fragile; callers must
keep the runner's live wait/recheck and treat any subsequent below-threshold
read as retryable (`EX_TEMPFAIL`), never as a study failure. A stronger local
policy may require `free_mib >= 2560`, but that is not the frozen S06 predicate
and was not applied here.

Evidence commands: `nvidia-smi --query-gpu=...`,
`nvidia-smi --query-compute-apps=...`, `ollama ps`, and
`systemctl --user is-active` for the three managed services.
