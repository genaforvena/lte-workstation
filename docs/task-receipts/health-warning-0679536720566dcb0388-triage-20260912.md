# Health-warning triage: `health-warning/0679536720566dcb0388`

- Checked: `2026-09-12T11:35Z`
- Owner: `health` on `mesh-home`
- Task: `health-warning/0679536720566dcb0388/triage`

## Verdict

This is a terminal sender-side delivery expiry, not evidence of a current
`genome` outage. Message `128253d43291f069` was an 11:14:59Z chat-review note
about `mesh-land` misclassifying the source-only `mesh-manifest-reader.sh`.
Delivery reached its 900-second bound at 11:30:14Z with zero successful sends.
The same manifest-classification work is already covered by the open genome
step `tg-scripts-layout-migration-20260912/manifest-autowire-land-vitality`,
so the expired notification did not strand that implementation obligation.

The retained evidence cannot identify why no send succeeded. The deliverer
increments `attempts` only after `mesh-tell` succeeds, and does not log
per-minute idle-gate skips or failed tell calls. There is no ACK for this ID.
Thus receiver-side awareness of this particular FYI is unknown, while the
underlying task remains explicitly routed and open.

## Evidence

- `/home/mesh-home/.mesh/chat.log:55885` contains the exact source row and
  message ID. Its full text points to
  `docs/chat-range-reviews/witness-chat-range-review-near-55692-55782.md`,
  which documents the existing genome step and the checked-manifest finding.
- `/home/mesh-home/.mesh/chat-deliver-ledger.json` records the ID as
  `status=failed`, `terminal_reason=age-expiry`, `attempts=0`,
  `first_seen=2026-09-12T11:14:59Z`, `failed_at=2026-09-12T11:30:14Z`, and
  `failure_emitted=true`.
- `/home/mesh-home/.mesh/chat-deliver.log:2523` records age `908s`, target
  `genome`, and zero attempts. Exact-ID ACK search in retained `chat.log` and
  `tell-wal.log` found none.
- `mesh-task status tg-scripts-layout-migration-20260912` reports
  `manifest-autowire-land-vitality` still open under `genome`.
- Live crontab runs `.local/bin/mesh-chat-deliver` every minute. Source and
  deployed binary SHA-256 both equal
  `dbab9c4caaf506178bcf83fb0579555b6ec846015e6155dcd1d461fcf32b62ae`.

## Disposition

Close this warning as a historical terminal age expiry. Do not retry the stale
chat-review record or alter delivery policy based on this single event. The
known observability gap is that zero attempts does not distinguish repeated
idle-gate skips from `mesh-tell` failures; a future recurring cohort would need
that evidence before assigning a cause or changing behavior.

## Verification

- `python3 scripts/mesh-chat-deliver --test` — PASS.
- Exact message, ledger, ACK, target-task, cron, and deployed-binary checks —
  recorded above.
