# Health roll-call triage: Note 3 transport recovery

Task: `health-warning/85b8439744257e9a7796/triage`  
Source warning: 2026-09-12T11:43:11Z roll-call

## Current evidence at 2026-09-14 19:54Z

- `adb devices -l` lists the exact Note 3 serial `4d00553d61ab90b7` as an attached USB device
  (`model:SM_N900`).
- Bounded reads to that exact serial returned `SM-N900` from `getprop ro.product.model`, an uptime
  of 2 days 8:32:54, and battery-service state `USB powered: true`, `present: true`, `level: 100%`.
  This directly confirms that the Note 3 is reachable and its basic ADB read path currently
  responds.
- The prior `note3-live-health-reconciliation-20260912.md` documents real sensor reads and ADB
  transport recovery after two USB disconnects on 2026-09-12, followed by five-minute transport
  probes reaching the device. This current probe does not establish uninterrupted transport or
  provide fresh motion/orientation/magnetometer/light samples.

## Disposition

The old `Note3 UNKNOWN` line is stale for current ADB reachability and basic battery visibility.
It is not evidence that transport stayed continuously available: the prior two disconnects and
recovery sequence remain the known intermittent USB blind. No new disconnect or failed probe was
observed, so no device or USB configuration change is justified. Resume observation only if a
fresh bounded ADB probe fails or the scheduled transport tape records another disconnect.

## Verification

- `adb devices -l`: only the attached Note 3 serial is listed as `device`.
- `adb -s 4d00553d61ab90b7 shell getprop ro.product.model`: `SM-N900`.
- `adb -s 4d00553d61ab90b7 shell uptime`: successful.
- `adb -s 4d00553d61ab90b7 shell dumpsys battery`: USB powered, present, 100%.
- Read the exact prior live-health reconciliation receipt; no sensor readings beyond the listed
  ADB and battery checks are claimed for this turn.
