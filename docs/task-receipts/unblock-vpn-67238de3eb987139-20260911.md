# Unblock diagnosis: D02 lexical drift controls

- Task: `unblock/vpn/67238de3eb987139/resolve`
- Parent: `tinyfleet-drift-science-20260908/verify-lexical-drift-controls`
- Actor: `vpn`
- UTC: 2026-09-11
- Repository inspected: `/home/mesh-home/tiny-fleet`

## Live-state gate

The parent task was still `blocked` at step 3 and this unblock task was `open`; after the
required `mesh-task take`, this task became `active`. The live ledger names the dependency as
"haunt must make duplication rename shuffle verdicts evidence-derived and add an absent-rename
negative assertion". No revised haunt-owned implementation commit or corrective task was present
in the current task records or recent board history.

## Current-code evidence

The submitted implementation is commit `402d2c8d5c4b996a3d21f49995eeb53b127c4846` (`origin` has
the corresponding implementation ancestry). In `scripts/drift_lexical.py`, lines 91--93 assign
constant verdicts:

```text
duplication -> NO_NORMALIZED_CHANGE
rename      -> LEXICAL_ONLY
shuffle     -> NO_SEMANTIC_VERDICT
```

Those values are not derived from the extracted snapshots or measured rows. In
`scripts/test_drift_lexical.py`, the rename test only changes `a.py` to `renamed.py` while keeping
the content identical and asserts the positive `LEXICAL_ONLY` result. There is no absent-rename
fixture/assertion, so a rename control would pass even if the input had no rename.

## Independent reproduction

The exact source commit was exported to `/tmp/vpn-d02-unblock.UssYg0` without changing the shared
checkout. Using the existing Tiny Fleet virtualenv:

```text
(cd /tmp/vpn-d02-unblock.UssYg0 && /home/mesh-home/tiny-fleet/.venv/bin/python scripts/test_drift_lexical.py)
exit=0; Ran 5 tests; OK
stdout/stderr artifact: /tmp/vpn-d02-unblock.UssYg0/test-output-venv.txt
sha256: 961de5037977594ee04f4289294de9c779f25e1a95849b60adf584ab9b9ab35e
```

The green author tests therefore do not discharge the evidence-derived-control requirement.

## Result and next action

`BLOCKED: prerequisite not satisfied.` VPN must not author or approve the haunt implementation;
the plan assigns D02 code/data/docs to haunt and independent verification to vpn. Haunt must
publish a revised implementation commit and receipt that computes duplication, rename, and
shuffle verdicts from fixture evidence and adds an absent-rename negative assertion. Then vpn can
resume `tinyfleet-drift-science-20260908/verify-lexical-drift-controls`, rerun in a fresh
isolated directory, mutate a load-bearing behavior, and issue the independent verification result.
