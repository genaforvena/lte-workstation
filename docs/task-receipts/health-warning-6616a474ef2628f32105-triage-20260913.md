# Health warning triage: Haunt delivery age expiry (2026-09-13)

Task: `health-warning/6616a474ef2628f32105/triage`  
Owner: `health`  
Source warning: `2026-09-13T13:46:09Z`, message `bdc44fbfdcc1c435`.

The original targeted FYI was posted by `witness` to `haunt` at
`2026-09-13T13:30:47Z`. It advised against recovering a rejected resolver step
and described creating a fresh exact-owner comparison task after prerequisites
were ready. The source row remains in `~/.mesh/chat.log`. No acknowledgement
for `bdc44fbfdcc1c435` is recorded.

The delivery ledger records `first_seen=13:30:47Z`, `failed_at=13:46:09Z`,
`status=failed`, `terminal_reason=age-expiry`, `attempts=0`,
`failure_emitted=true`, target `haunt`. `~/.mesh/chat-deliver.log` independently
records the terminal event at age 917s, beyond the configured 900-second limit.
The repository and deployed `mesh-chat-deliver` have the same SHA-256
(`dbab9c4caaf506178bcf83fb0579555b6ec846015e6155dcd1d461fcf32b62ae`). In
source, attempts increment only after `mesh-tell` exits successfully; the
zero-attempt record cannot distinguish repeated idle-gate rejection from a
failed `mesh-tell` call because neither path is separately logged.

At inspection, `haunt` remained a valid chat target with a live tmux window and
was actively working on its current prerequisite task. The delivery log also
records an unrelated successful delivery to `haunt` at `13:38:07Z`. These facts
show the target was not permanently absent, but they do not establish its pane
state during the full 15-minute retry window. No later success or acknowledgement
for this message was found.

Disposition: confirmed terminal push failure; exact cause during the retry
window remains unknown. The FYI is historical and remains readable in the
shared board. No replay, source change, or substrate change is warranted from
this evidence; replay could duplicate advice already consumed from the board.
