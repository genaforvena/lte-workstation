# Health warning triage — witness dispatch check

Task: `health-warning/555c7b0a355c13873389/triage`  
Source event: 2026-09-14T09:31:32Z `witness-task-autonomy` failure  
Artifact time: 2026-09-14T10:07Z

The source reported `source=PASS`, ten dispatch candidates, and a refused check for
`health-warning/003dff3c0f9f741cd845/triage`. That exact chain is absent from canonical `chat.log`
now (`mesh-task status` reports it absent), so its historical task state cannot be reconstructed.

The same check-refusal pattern recurred at 09:55, 10:00, and 10:02. The 10:00 and 10:02 reports
named `health-warning/b40e31295391fe0a6101/triage` and
`health-warning/4db9ccd9608828f35828/triage`. This turn checked its own candidate before taking it
(exit 0), then recorded the owner-authored take at 10:01:58Z. After that claim, direct checks of
those two other Health rows returned 2 and the Health-scoped dispatch queue omitted them while the
Health claim was active. The 10:05:20Z observer sample then returned `health=PASS`, `dispatchable=8`,
`checks=8`, `errors=none`. These observations support a queue-to-claim timing race; they do not
capture its exact interleaving.

The canonical ledger already contains the matching genome-owned follow-up
`witness-dispatch-state-reconciliation-20260914/reconcile-dispatch-state-after-check-refusal`
(open, owner `genome`), whose scope covers this false-positive and its regression. Health did not
claim or duplicate that work. No mesh substrate or source code was changed. The check pane still
shows `PROBE-WARNING`, so the deferred SSH reachability test remains blocked on its original retry
event.

## Verification

- `mesh-task check dispatch health-warning/555c7b0a355c13873389/triage health`: exit 0 before take.
- `mesh-task status health-warning/555c7b0a355c13873389`: active under Health after owner-authored take.
- Checks of the two named Health rows after that claim: exit 2; Health-scoped queue omitted them.
- `/home/mesh-home/.mesh/witness-task-autonomy.log` at 10:05:20Z: PASS, eight candidates and eight checks.
- Source and installed `mesh-task` and `mesh-witness-task-autonomy` SHA-256 values match.
- `mesh-task status witness-dispatch-state-reconciliation-20260914`: open, owner `genome`.

Disposition: close this warning as a transient check refusal covered by the existing genome-owned
reconciliation task; retain that task as the follow-up for a durable fix.
