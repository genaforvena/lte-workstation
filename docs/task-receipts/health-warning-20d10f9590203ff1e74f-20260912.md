# Health warning triage: DNS resolver and answer delta

Task: `health-warning/20d10f9590203ff1e74f/triage`

The warning reports the DNS values `213.87.2.89` and `217.66.16.35` after a prior `160.79.104.10`
observation. Current checks distinguish resolver addresses from the DNS answer:

- `resolvectl status` lists `213.87.2.89` and `217.66.16.35` as DNS servers on `enp42s0`. On
  `tailscale0` it lists `100.100.100.100` and `fd7a:115c:a1e0::53`, with `~.` routing all names
  there. `ip route get` sends both `enp42s0` DNS server addresses through `tailscale0 table 52`.
- Direct `dig` queries to `213.87.2.89` and `217.66.16.35` timed out. Queries through
  `100.100.100.100`, `1.1.1.1`, and `8.8.8.8` returned `api.anthropic.com A 160.79.104.10`;
  `resolvectl query` also returned that same A answer via `tailscale0` (plus AAAA
  `2607:6bc0::10`). Thus the reported 213/217 values are resolver addresses, not a changed A
  answer; the prior 160.79.104.10 answer is still reproduced. The physical-link DNS servers are
  not reachable by these direct probes over the current route, while the tailnet and public
  resolver probes succeeded.
- The latest `mesh-dash --once check` at 13:54:41Z reports current egress `OK loss=0%`,
  supervised `4UP/0DOWN`, and Tailscale path `OK peers=8 direct=2 relay=0`. `phaedra` remains the
  direct active exit node, so the exit-node SPOF remains. The current pane says `imac-rozalia`
  answers ICMP but has no SSH. The earlier relay observation was transient and later Tailscale
  status showed it direct/active.
- `mesh-lan-presence --nodes` at 13:54Z still reports UNKNOWN because this host has no address in
  `192.168.8.0/24`, cannot ARP that segment, and no known host answered ICMP.
- The pane's doctor result is still cached from 13:35:58Z (`3F/34W`); the prior live sweep did
  not complete, so no fresh total is claimed. The selected mic source remains the USB2.0 Camera,
  currently occupied by `arecord`; I did not start a competing capture.

Verdict: current A resolution matches the prior answer despite the resolver-address delta. The two
physical-link resolver IPs did not answer direct probes over the current Tailscale route; tailnet
and public DNS paths returned the expected answer. Egress is passing now, LAN remains UNKNOWN,
and the configured exit node remains a SPOF. No DNS, route, VPN, firewall, or audio setting was
changed; these are read-only observations for the network/DNS single-writer to reconcile if the
link resolvers are expected to be used.

Verification: `mesh-dash --once check` at 13:54Z; `resolvectl status`; `ip route get` for both
configured link DNS servers; direct `dig` to each (timed out); `dig` through `100.100.100.100`,
`1.1.1.1`, and `8.8.8.8`; `resolvectl query`; `tailscale status`; and `mesh-lan-presence --nodes`
(exit 1/UNKNOWN).
