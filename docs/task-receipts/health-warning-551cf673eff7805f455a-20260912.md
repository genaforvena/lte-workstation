# Health warning triage: historical egress and LAN delta

Task: `health-warning/551cf673eff7805f455a/triage`

The warning describes a 2026-09-10 snapshot. Current read-only checks show that its peer-path
details have since changed again:

- The live `tailscale status` at 13:46Z reports `phaedra` active as the exit node over a direct
  path (`38.49.216.141:41641`); `imac-rozalia` is idle, not active through relay `hel`. The
  configured exit-node dependency remains a single point of failure.
- The 13:46 check pane still reports egress on `tailscale0`, supervised `4UP/0DOWN`; its 24-hour
  line reads `BAD:0%(0/439)`. The local `ip route` read shows the system default route through
  `enp42s0`; these views do not establish which route a fresh end-to-end egress probe used, so I
  preserve the monitoring discrepancy rather than infer a route change.
- `mesh-lan-presence --nodes` exited 1 and reported UNKNOWN: this host has no address in
  `192.168.8.0/24`, cannot ARP that segment, and received no ICMP response from a known host. That
  does not prove the router is down.
- The cached doctor data in the pane is timestamped 13:35:58Z and still lists the historical
  egress warnings. The cached artifact is not a fresh doctor run; a fresh `mesh-doctor` run stalled
  during smoke tests and was interrupted after no new output. No doctor refresh is claimed.

Verdict: the peer-path part of the warning is stale; current Tailscale state is direct/active for
the exit node and idle for `imac-rozalia`. LAN remains unobservable from this host. A route/egress
discrepancy remains in the pane and local route table, so I made no routing, VPN, DNS, or firewall
change. The next safe action is a fresh one-shot pane read and, if the discrepancy persists, a
single-writer owner can reconcile the egress probe and route diagnostics.

Verification: `mesh-dash --once check` at 13:46Z; `tailscale status`; `ip route`; and
`mesh-lan-presence --nodes` (exit 1/UNKNOWN). `mesh-doctor` did not complete, so its cached result
is explicitly treated as stale.
