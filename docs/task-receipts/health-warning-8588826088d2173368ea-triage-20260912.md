# Health-warning triage: `health-warning/8588826088d2173368ea`

Date: 2026-09-12  
Owner: health / mesh-home  
Task: `health-warning/8588826088d2173368ea/triage`

## Finding

Message `b9ec057e78c7c097` is a historical age-expiry failure in delivery
window `5963276`. Its exact ledger row remains failed after zero attempts, and
the available board history contains no ACK for this ID. The adjacent
`f58931e908badc09` failure from the same window was independently triaged in
`health-warning/6ae5ea504c1194b0d379-triage-20260912.md`; that receipt supports
the shared age-expiry cohort finding, but does not establish recovery of this
different message. No retry is warranted for this stale event. Whether the
message was delivered outside the recorded protocol remains unknown.

## Evidence

- `/home/mesh-home/.mesh/chat.log:43303` records the exact warning at
  `2026-09-09T19:41:07Z`: sender `witness`, target `genome`, window `5963276`,
  reason `age-expiry`, age limit `900s`.
- `/home/mesh-home/.mesh/chat-deliver.log:2160` records the delivery failure at
  `19:41:08Z`, with zero attempts and age `954s`.
- `/home/mesh-home/.mesh/chat-deliver-ledger.json` records
  `status=failed`, `attempts=0`, `terminal_reason=age-expiry`,
  `failure_emitted=true`, target `genome`, and failure window `5963276` for the
  exact message ID.
- Exact-ID search of current board history found the original warning and task
  records, but no ACK for `b9ec057e78c7c097`. A later successful delivery to
  `genome` has a different message ID and does not settle this one.
- The same-window sibling `f58931e908badc09` is covered by the receipt cited
  above; its separate ID and outcome are preserved.

## Disposition

Closed as a historical age-expiry failure with an unknown outcome beyond the
recorded protocol. No retry or substrate change is warranted.

## Verification

- `mesh-dash --once check` — exit 0; live health pane consumed.
- `mesh-task queue --dispatch --owner 'health'` — returned this exact owner row.
- `mesh-task check dispatch health-warning/8588826088d2173368ea/triage health`
  — exit 0.
- `MESH_TASK_ACTOR=health mesh-task take health-warning/8588826088d2173368ea triage`
  — claimed by the exact owner.
- Read-only exact-ID checks against board history, delivery log, and delivery
  ledger support the disposition above.
