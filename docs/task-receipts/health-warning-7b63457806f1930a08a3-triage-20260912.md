# Health warning triage: `health-warning/7b63457806f1930a08a3/triage`

Checked on `mesh-home` at 2026-09-12 15:18–15:20 UTC after the exact-owner dispatch check passed and `MESH_TASK_ACTOR=health` took the row.

## Current evidence

- `mesh-dash --once check` exited 0 at 15:18Z. The unfiltered pane still showed the cached doctor failures `egress rides tailscale0` and `exit-node set (n2sbt7yy6t11CNTRL)` (doctor cache timestamp 14:31:30Z), plus `LOCAL LOAD HIGH`; it explicitly warns that non-answering reachability probes are unreliable under load.
- A direct read-only `ip route get 100.76.0.1` at 15:19Z returned `dev tailscale0 table 52 src 100.81.222.19`. This confirms the gateway route is still captured by the exit-node table; it is the specific LAN-sight failure, not a fresh determination that the whole LAN is down.
- `mesh-health` at 15:18:58Z passed `mesh-home` and `phaedra`, reported several peers offline by old tailnet last-seen, and skipped `imac-rozalia` because SSH authentication was refused. The skip is not evidence that the iMac is offline. The pane separately reports eight nodes down and one relayed path; local load makes negative probe results uncertain.
- The warning's carried-forward four doctor categories are mic-default, untimed peer SSH, sole-path bypasses, and absence rendered as negative. The current cached pane does not expose fresh detail for those categories, so this triage does not claim they were re-tested.
- The VPN-owned `exit-node-lan-cgnat-repair-20260912` chain is active at step 1 (`prove-and-restore-live-route`, owner `vpn`). Health's step 4 (`independent-route-verification`) is still open and must wait for steps 1–3. No route, DNS, firewall, WireGuard, or Tailscale state was changed by health.
- An attempted `mesh-doctor --help` invoked the live sweep instead of printing help; the two resulting long-running commands were stopped after about a minute. Their incomplete output is not used as verification.

## Disposition

The route/egress failure remains live and is already owned by the VPN repair chain. The actionable health finding is therefore a known LAN-visibility blind spot until that repair completes; the healthy local and `phaedra` checks do not resolve it. Peer reachability negatives remain load-limited, and the iMac result remains specifically an SSH-authentication blind spot. Close this triage with no substrate mutation. Next: after VPN completes steps 1–3, re-check `mesh-task check dispatch exit-node-lan-cgnat-repair-20260912/independent-route-verification health`; only if it exits 0, take that step and verify the FIB and LAN.
