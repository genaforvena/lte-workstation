# Health warning retry receipt — 2026-09-15

Task: `health-warning/1372b4720375a95976c7/retry-20260915/triage`
Successor of rejected: `health-warning/1372b4720375a95976c7/triage`
Gate: `20260914T190000Z-210000Z/analyze-observation`

## Fresh live classification

At 2026-09-15T13:08Z, `mesh-dash --once check` reported:

- local supervised `4UP/0DOWN`, organs `13LIVE/0DARK`, and current egress OK;
- fleet `4 ssh / 2 LAN / 4 down`, with `PROBE-WARNING: LOCAL LOAD HIGH` and
  reachability probes explicitly unreliable;
- cached doctor `FAIL=1 WARN=33`, including one recent dispatch.log error line;
- GPU CRITICAL at 88% VRAM, while load-audit classified the transient CPU load
  as `JUNK-TRANSIENT` and the GPU as `GPU-ORGAN`.

`mesh-health` independently confirmed the local node, iMac-Rozalia, and Phaedra
reachable, with ilya, two imozerov nodes, and rip offline by the tool's probes.
`mesh-verify` confirmed local linger/tools/session/connectivity; ideapad remained
unreachable. These are probe-limited observations, not bounded evidence for a
local routing, DNS, firewall, VPN, or service repair. No substrate state changed.

## Result

The stale duplicate was correctly replaced by this fresh exact-owner triage.
The current warning is retained as a known blindness/observation (especially
the explicit probe unreliability and cached doctor age); no comparison or
substrate mutation is safe on this frame. The completed historical gate and
resolver receipt remain linked in the successor description.

## Verification

- `mesh-dash --once check` exited 0 at 13:08:47Z.
- `mesh-health` exited 0 at 13:08:37Z.
- `mesh-verify` completed its local row and reported ideapad unreachable.
