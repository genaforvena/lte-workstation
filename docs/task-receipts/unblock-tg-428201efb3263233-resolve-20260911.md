# Unblock receipt — `unblock/tg/428201efb3263233/resolve`

Captured 2026-09-11T22:16Z UTC by owner `tg`.

## Result

The resolver description is stale about implementation: the current source and deployed copy
contain `valid_source`, `cut_window`, `collage_build`, and the collage tick path. The blocker is
acceptance evidence, not missing helpers. This turn produced a fresh state audit and attempted the
required gates, but the live sound reflex held `records.log.reflex.lock`; the honest smoke test
returned `rc=2` (`smoke-test: n/a`) instead of attributing sandbox writes. No source or wiring
change was made.

## Evidence

- `mesh-task check dispatch unblock/tg/428201efb3263233/resolve tg`: `rc=0`.
- Source/deployed SHA-256 match:
  `030160982a5f9324b68a9677c728e32a0d6ddc27b5afae004dcdb8361324ce44`.
- `scripts/mesh-sound-reflex` lines 384, 393, 407, and the collage path are present.
- `mesh-sound-reflex --status`: ledger 2301 lines / 171 pending; 4711 renders.
- Fresh `timeout 300 bash scripts/mesh-sound-reflex --test`: `rc=2`,
  `smoke-test: n/a (a live sound-reflex tick holds ...records.log.reflex.lock...)`.
- A fresh real ambient artifact exists at
  `/home/mesh-home/.mesh/records/20260911-221032-ear-262b420f.wav` (576044 bytes), but it is
  an input capture, not the required settled collage MP3/ledger receipt.

## Disposition

`BLOCKED/dependency`: mutation-red and reflex-owned settled collage evidence remain unproven in
this quiet window. The exact next action is to rerun the three scratch mutations and the full
source/deployed smoke tests after the live reflex releases the lock, then capture a settled
collage MP3 plus its ledger/params row and rerun the six-task checklist. No false unblock is
claimed.
