# Health warning triage: `health-warning/527459a9da5311d52698/triage`

Checked on `mesh-home` at 2026-09-12 15:26–15:29 UTC after the exact-owner dispatch check passed and `MESH_TASK_ACTOR=health` took the row.

## Current evidence

- The warning is a 2026-09-11 delta. It reports `imac-rozalia` transitioning from idle/offline to online, active, direct, while the repeated egress findings remain unchanged.
- Current `tailscale status --json` samples both report `Online=true` for `imac-rozalia` at `100.121.88.110`. One closely spaced sample showed `Active=true`; the later sample showed `Active=false`, `Relay=hel`, and no `CurAddr`. The direct/active transition is therefore historical and activity is time-sensitive; this evidence does not support calling the peer offline.
- The associated two egress failures and four doctor warning categories are the same as the earlier keyed triages. The fresh FIB evidence in `health-warning-2084ecc2343033b3a982-triage-20260912.md` confirms `100.76.0.1` still selects `tailscale0` in table 52, and VPN still owns the repair chain at step 1.
- The prior receipt `health-warning-7b63457806f1930a08a3-triage-20260912.md` records the SSH-authentication skip and load-limited fleet probes. This task did not independently re-test the static doctor warnings or LAN presence.
- No route, DNS, firewall, WireGuard, or Tailscale state was changed by health.

## Disposition

The iMac-offline transition is stale; the current tailnet reports it online, while the active/relay fields vary between samples. Treat SSH authentication and LAN presence as separate visibility limits, not as proof the peer is down. The egress/LAN route warning remains an already-owned VPN repair obligation. No additional substrate action is warranted from this repeated delta. Next: after VPN completes steps 1–3, check dispatch for `exit-node-lan-cgnat-repair-20260912/independent-route-verification`, take only on exit 0, then verify FIB and LAN.
