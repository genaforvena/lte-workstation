# Unblock receipt — `unblock/tg/9633038520af8042/resolve`

Captured 2026-09-11T21:28Z UTC by owner `tg`.

## Result

The parent external-event blocker is cleared by fresh operator inbound at
`2026-09-11T19:01:31Z`. The parent was resumed with that event. The live
delivery audit still fails the broader acceptance condition: retained failed
and expired-preledger rows remain, so no settlement is claimed.

## Evidence

- `mesh-dash --once tg`: `unit=active/running`, `queue=0`, `conflict=0`,
  newest operator inbound `2026-09-11T19:01:31Z`.
- `/home/mesh-home/.mesh/chat-deliver-ledger.json`: target `tg`, 253 rows:
  `acked=188`, `failed=61`, `expired-preledger=4`, `pending=0`,
  `awaiting-ack=0`; SHA-256
  `00b8905d73c6936ff4faf22ce97417efeda0f67472d8d2b46ed58cd1071aafaf`.
- `/home/mesh-home/.mesh/chat.log` SHA-256:
  `af4a800b6fbb3d44af5dadd8b2e3feb71fbd89cabd147de1dd92d4e6d71cff30`.
- `scripts/mesh-chat-deliver --test`: PASS.
- `bash tests/test-mesh-chat-deliver.sh`: PASS.
- `python3 tests/test-mesh-chat-deliver-attempts.py`: PASS.

## Disposition

The exact resolver is DONE with this receipt. The parent is resumed, remains
unresolved on exact failed-ID to one-to-one answer reconciliation, and must not
be terminalized until that evidence exists.
