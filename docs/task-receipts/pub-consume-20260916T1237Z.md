# pub consume receipt — dev.to comment queue — 2026-09-16

## Live evidence

`mesh-dash --once pub` was run at 2026-09-16T12:37:37Z. It directed the window to the
dev.to reply queue and showed five owed entries. A fresh `mesh-devto-comments --list`
confirmed these comment IDs and contents:

| comment | author | disposition |
|---|---|---|
| `3ekdf` | `@jasonbotsford0239` | promotional API pitch with TinyURL; no reply |
| `3eoo9` | `@nwfd_engineer` | unsolicited service offer with checkout URL; no reply |
| `3ekc9` | `@elbert3255` | job-application promotion with TinyURL; no reply |
| `3ef25` | `@stephanheidenreich761` | unrelated API promotion with short URL; no reply |
| `3ecl5` | `@fromzerotoship` | substantive technical claim; draft a verification question |

## Action and delegation

Five independent read-only triage analyses were delegated to `devto-raknaos`, `devto-nwfd`,
`devto-elbert`, `devto-stephan`, and `devto-fromzero`. I personally inspected each returned
turn through the relay. The workers agreed that the four shortened-link promotions should not
receive a public reply; the `3ecl5` worker supplied a bounded question requesting source,
toolchain flags, linker script, ROM images, or disassembly.

The inspected operator-ready draft is preserved in
`docs/drafts/devto-replies-20260916.md`. No worker posted, edited the repository, or received
credentials.

## External boundary and retry edge

`mesh-devto-comments --help` documents that the public dev.to API is read-only for comments;
there is no supported write-comment endpoint. Therefore this turn made no public mutation.
The bounded actuator check `timeout 12s mesh-devto-reply --owed --json` returned `rc=124`
with no result, so actuator eligibility remains UNKNOWN rather than being treated as an empty
queue.
The four spam entries should be removed/reported through the authenticated browser moderation
path when that capability is available. The genuine `3ecl5` response should be posted only
after the actuator resolves the comment to a numeric ID and a fresh nested-comment receipt is
observed. Do not claim the queue is cleared from this artifact.
