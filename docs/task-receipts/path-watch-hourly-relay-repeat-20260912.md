# Path-watch hourly relay alerts — 2026-09-12

## Finding

The three board alerts at 15:49:02Z, 16:49:01Z, and 17:49:01Z recur at
approximately the 3600-second relay cooldown floor. Their chat timestamps are
3599 seconds apart, then 3600 seconds apart; the gate records its epoch before
calling `mesh-chat`, which stamps the board line afterward. The retained path
tape records direct recoveries and new relay crossings between alerts, so they
are distinct observed episodes rather than one continuously unresolved episode
being re-announced.

The netcheck tape records `udp=true` at 15:44:02Z, 16:49:01Z, and 17:49:01Z,
so these samples do not indicate a local UDP failure explaining all peers'
relay state. The live `mesh-path-watch --status` read at 19:04:01Z reported
`imac-rozalia=relay`.

## Diagnosis, code, and wiring evidence

The full inter-alert tape shows each later post followed a real recovery and a
new debounced direct→relay crossing:

| Alert | Tape since preceding alert |
| --- | --- |
| 15:49:02Z | 15:34 direct recovery, 15:44 relay crossing |
| 16:49:01Z | 16:14 direct recovery, 16:24 relay crossing, 16:39 direct recovery, 16:44 relay crossing |
| 17:49:01Z | 17:19 direct recovery, 17:44 relay crossing |
| after 17:49:01Z | 17:54 direct recovery, 18:04 relay crossing |

So these are distinct relay episodes, not one unresolved episode announced
again after an hour. The same-peer cooldown still applies across recovery: the
fourth field `last_alert_epoch` is preserved when mode returns to direct, and
`relay_alert_due` suppresses same-peer crossings until 3600 seconds have elapsed.
`relay_now` is captured before the chat post, while the board timestamp is
generated by `mesh-chat`; the one-second difference between displayed alert
times does not indicate a cooldown bypass. The earlier completed repair remains
correct. Extending suppression across these real recurrences would risk hiding
a later relay transition, so no code change is warranted.

Verification on 2026-09-12:

- `scripts/mesh-path-watch --test` — passed on 2026-09-12, including a real
  live status read and fixtures for recovery carrying cooldown, same-peer
  suppression, and new-peer escalation.
- `sha256sum scripts/mesh-path-watch ~/.local/bin/mesh-path-watch` — both
  `384ba31d7dae563cccf8b0ab024d5b812e69529202868fb3b223ff636ff8cf97`.
- Live crontab wiring: `72:4-59/5 * * * * $HOME/.local/bin/mesh-path-watch`
  appending to `~/.mesh/path-watch.cron.log`.
- Live status read: `OK 2026-09-12T19:04:01Z`, with the iMac currently on
  relay.

The previous cooldown implementation and its test are documented in
`docs/chat-review-mesh-path-watch-relay-crossing-no-episode-cooldown-20260909.md`.
