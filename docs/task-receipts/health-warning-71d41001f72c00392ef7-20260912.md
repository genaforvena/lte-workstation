# Health warning triage: egress, LAN reachability, and peer change

Task: `health-warning/71d41001f72c00392ef7/triage`

The check-stream warning describes persistent known state rather than a new substrate failure: the
doctor artifact remains at 2 FAIL / 33 WARN with the same two egress findings (host traffic uses
`tailscale0`; the configured exit node is a single point of failure), LAN presence remains UNKNOWN,
and DNS is unchanged. Its only peer delta was `imac-rozalia` becoming active through relay `hel`.

Current observations confirm that reading:

- The 13:25Z live check pane reported egress `OK`, loss 0%, and two nodes reachable (mesh-home and
  phaedra); LAN peers remained 0 and eight tailnet nodes were down/unknown. The doctor results were
  cached at 12:32Z, so their `2 FAIL / 33 WARN` count is a baseline artifact, not a new live scan.
- `tailscale status --json` currently reports the configured exit-node ID `n2sbt7yy6t11CNTRL`
  online at `100.94.116.17` (phaedra), and `imac-rozalia` online, active, relayed via `hel` at
  `100.121.88.110`. The reported relay transition is present; it does not show a new loss of
  reachability.
- A fresh `mesh-lan-presence --nodes` read exited 1 and returned UNKNOWN: this node has no local
  address in `192.168.8.0/24`, and neither ARP nor ICMP found a known host. This is an honest blind
  from this segment, not proof that the router is down.
- `mesh-egress-health` exited 0 without a diagnostic; the live pane's independent probe showed
  0% loss. No route, DNS, firewall, VPN, or exit-node setting was changed.

Verdict: the intentional Tailscale egress path is currently working, with a known single-exit
dependency; LAN presence remains unverified from this node. `imac-rozalia` is currently online via
relay and does not create a new failure. Preserve substrate state. The health warning is a repeat of
the known egress/LAN condition plus a peer-path observation, not a reason to reconfigure routing.

Verification: compared the 12:23Z cached doctor artifact with the 13:25Z live pane; queried current
Tailscale peer/exit-node state; ran the LAN-presence probe and recorded its UNKNOWN/exit-1 result;
and ran `mesh-egress-health` (exit 0).
