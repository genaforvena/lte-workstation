# Health warning triage: retired route proposal and current reachability

Task: `health-warning/1864b64da4309196149b/triage`

The Sep 10 roll-call says `route:no | PROPOSE none (retired 2026-09-07T08:59:10Z)`. This is a
status report and explicitly carries no live route proposal. I did not revive or apply a route
change.

The latest one-shot pane at 13:57:14Z reports egress on `tailscale0`, supervised loops `4UP/0DOWN`,
current egress probe `OK loss=0%`, and `PATH OK peers=8 direct=2 relay=0`; `phaedra` remains online
as the active direct exit node, leaving the known single-exit dependency. The pane's doctor total
is cached at 13:35:58Z (`3F/34W`), so it is not a fresh full doctor result.

`mesh-lan-presence --nodes` at 13:57Z still reports UNKNOWN: this node has no local address in
`192.168.8.0/24`, cannot ARP that segment, and no known host answered ICMP. A route lookup for
`192.168.8.1` selects the physical uplink's next hop `100.74.0.1`; a lookup for the mesh gateway
`100.76.0.1` selects `tailscale0 table 52`. These observations do not establish that the router is
down or justify adding a route. Local load was elevated (`15.22/16` in the cached audit) and the
reachability pane showed `imac-rozalia` ICMP but no SSH, reinforcing the known visibility limit.

Verdict: the old proposal is retired, current public egress is functioning through the configured
exit node, and LAN/router presence remains UNKNOWN. No routing, VPN, firewall, or DNS state was
changed. The next safe step is another one-shot check; any route proposal must come from the
current substrate single-writer with evidence that a specific destination route is missing.

Verification: `mesh-dash --once check` at 13:57Z; `mesh-lan-presence --nodes` (exit 1/UNKNOWN);
`ip route get 192.168.8.1`; and `ip route get 100.76.0.1`.
