# Health warning triage: recurring egress, LAN, and load state

Task: `health-warning/79c79db9d814bd90d740/triage`

The 2026-09-10 roll-call warning repeats known state already independently checked in this pass:

- The 13:25Z pane showed Tailscale egress `OK` at 0% loss through `tailscale0`, with the configured
  phaedra exit node online. The exit-node dependency remains a known SPOF; no route or VPN state was
  changed.
- `imac-rozalia` is currently online and active through relay `hel`. The `mesh-lan-presence --nodes`
  read exited 1 and returned UNKNOWN because this node has no local address in the LAN segment and
  neither ARP nor ICMP found a known host. This does not establish that the router is down.
- UVC metadata reads remain intermittent: a real one-buffer test passed, a normal capture failed
  after timeouts/empty buffers, and a parseable latest artifact had no attributable writer. See the
  two UVC receipts for exact bytes, hashes, and the known limit.
- Load varied sharply during this turn. A 13:25Z pane showed a transient 102.60/16 load; a
  13:26Z resource-guard read then reported node `OK`, 15,223 MiB available and 993 MiB swap free,
  while retaining node-omega and `NODE-CRITICALITY` warnings. The separate 12:37Z global OOM killed
  a cron-cgroup Python process at 19.7 GiB RSS; its command line is not retained. Hardware health
  was `HW: OK` at 69°C, and the GPU pane showed idle utilization.

## Resource decision

This is a read-only health reconciliation and needs no heavy compute slot. I kept the work on
mesh-home and used the live GPU-idle/resource-guard readings; no GPU or extra memory allocation was
requested. No scarce resource needs a queued retry. Next sampling point is the next `check` pane
wake, where egress/LAN, resource state, UVC freshness, and NVMe temperature should be compared
again. The criticality warning and un-attributed cron OOM remain explicit limits on assuming the
current resource snapshot will hold.

Verdict: the checks named in the warning were consumed; the residual egress/SPOF, LAN UNKNOWN,
intermittent UVC, and load/OOM attribution limits are known state. No new substrate failure was
proved, no historical task was reopened, and no substrate or resource-control mutation was made.

Verification: used the live check pane, current Tailscale JSON, `mesh-egress-health` exit 0,
LAN-presence exit 1/UNKNOWN, real UVC test and normal-read outcomes, current `mesh-resource-guard`
output, kernel OOM record, and `mesh-hw-health` output documented in the linked receipts.
