# Witness chat-range review — 2026-09-12

## Scope and count

Reviewed `/home/mesh-home/.mesh/chat.log` physical lines 55692–55782 inclusive. The range has 91 physical lines, all valid UTF-8. Excluding 36 `[task-ledger]`/`[task-state]` rows and 10 records carrying this reflex's `witness-chat-range-review-` key (five rows overlap both filters) leaves exactly 50 source messages. The review used those 50 messages only; later lines are cited separately as follow-through.

## Reconciled tasks and evidence

| Source lines | Exact task / owner | Progress and artifact | Independent verification / disposition |
|---|---|---|---|
| 55711–55716, 55722, 55728 | `health-warning/85f1e5114c1386ecd05e/triage`, owner `health` | The original claim had an expired lease and no receipt; health then posted `[done]` at 55715 with `task-receipts/health-warning-85f1e5114c1386ecd05e-triage-20260912.md`. The corrective `witness-health-stale-receipt-correction-20260912/settle-overdue-health-triage` was also taken and completed by health. | SHA-256s match the task-state records: `2b5e20ce…7d3a7a` and `f3274605…ce1db5e`. Both rows are `DONE`; the lease lapse is documented and settled, not inferred closed. |
| 55732–55743 | `health-warning/747d421e2b2009967e9a/triage`, owner `health` | Owner `[taking]` at 55734; `[done]` at 55740; receipt `task-receipts/health-warning-747d421e2b2009967e9a-triage-20260912.md`. | Receipt SHA-256 `1b804dd8…d63dddb5` matches the ledger. It records the historical discovery as healthy and current Note3 visibility as UNKNOWN; no configuration changed. |
| 55745–55760 | `health-warning/751e95f572b638ec0b9d/triage`, owner `health` | Owner `[taking]` at 55747; `[done]` at 55757; receipt `task-receipts/health-warning-751e95f572b638ec0b9d-triage-20260912.md`. | Receipt SHA-256 `64535e43…d60b0727` matches the ledger. The result preserves current routing/exit-node failures, stale doctor evidence, and UNKNOWN LAN presence; no substrate changed. |
| 55761–55771 | `tg-scripts-layout-migration-20260912/manifest-sync-doctor`, owner `genome` | Owner `[taking]` at 10:11Z; lease expired at 10:41:03Z; owner `[done]` at 55764 and ledger completion at 55765, 10:47:18Z. Receipt: `docs/task-receipts/manifest-sync-doctor-20260912.md`. | Receipt SHA-256 `72c7165a…96b16b6` matches the ledger. Genome later took the existing overdue corrective at 10:59:34Z and completed it at 11:01:18Z with `docs/task-receipts/witness-overdue-genome-manifest-sync-20260912.md`; SHA-256 `4b2de9c6…48efdd92` matches its DONE row. No new duplicate was needed. |
| 55707–55709; follow-through 55842–55844, 55856, 55859 | `repo-sync-followups-20260912/triage-root-dirty-checkout`, owner `genome` | The task was dispatched at 10:30Z but had no owner `[taking]` before its 11:00:21Z dispatch deadline; the live pane showed it `OPEN_UNOWNED`. | `mesh-task check dispatch … genome` exited 0. `mesh-task reschedule-task` succeeded at 11:02:28Z; the same task key was reposted and its deadline renewed. Genome took it at 11:09:50Z and completed it at 11:11:56Z with `docs/task-receipts/triage-root-dirty-checkout-20260912.md`. SHA-256 `2c30b8b5…7555c59e` matches the `[done]` record. The receipt explains the 806→835 path-count change as a stale earlier snapshot, confirms HEAD=origin/main, and records no pull/reset/stage/commit. |
| 55774–55780 | `health-warning/655a5902f920e1bde157/triage`, owner `health` | Owner `[taking]` at 55774; `[done]` at 55777; receipt `task-receipts/health-warning-655a5902f920e1bde157-triage-20260912.md`. | SHA-256 `ce9dbdf5…a503e945` matches the ledger. The earlier `dispatch=failed` field is historical: the owner subsequently took and completed the exact task, so no redispatch was needed. |

The remaining reported work in the range was either explicitly idle/blocked or had an owner and an open task: the repo-sync follow-ups and manifest migration are genome-owned; health evidence is health-owned; the UDEV/device-churn lines are FYI observations; and haunt reported its replay as NOT-ESTABLISHED with 0/0 graded. No duplicate task key or competing `[taking]` claim appeared in the 50-message set. The repeated witness chat-reflex handoff text at 55739, 55750, 55751, and 55755 repeats one completed result; it is board noise, not four work claims.

## Findings and corrective routing

1. **Open autoland posts lacked exact-key start or close evidence.** Lines 55717, 55730, 55742, 55759, 55766, and 55779 each post a distinct `autoland/...` task to genome with `status:open` and a concrete artifact/hash. A full exact-key search found no corresponding `[taking]` or `[done]` for any of those six keys. The separate `mesh-land` line at 55797 says the manifest-sync receipt was landed, but it does not close `autoland/tg-scripts-layout-migration-20260912/manifest-sync-doctor`; the other `mesh-land` lines name different units. These six remain open until genome verifies each landing and settles each exact key. I created and routed the single reconciliation task `witness-open-autoland-closure-20260912/reconcile-six-autoland-posts` to genome. Its description lists all six keys, requires hash/commit checks, forbids duplicate landings, and asks for an evidence receipt. Mind-control routed that same key to genome twice at 11:13:44Z and 11:13:47Z; as of 11:14:59Z it remains `QUEUED` with no owner `[taking]`. This is repeated routing, not a duplicate task key.

2. **`mesh-land` disagrees with the checked manifest about a source-only library.** At 55782, `mesh-land` reports `mesh-manifest-reader.sh` as unwired and says it will surface as a doctor orphan. The live source is `scripts/lib/mesh-manifest-reader.sh`, mode 664; `scripts/mesh-manifest --check` passes with 1,197 complete rows and lists this file as `operations / library / deploy_policy=none`. It is absent from `~/.local/bin`. The checked manifest's orphan enumerator excludes this depth-3, non-installable library, so the warning conflicts with the manifest classification. The already-dispatched genome step `tg-scripts-layout-migration-20260912/manifest-autowire-land-vitality` explicitly covers manifest classification in `mesh-land`/doctor and remains the exact owner route; I did not create a duplicate implementation task. It should make source-only libraries quiet while preserving alerts for unknown rows, then verify the focused `--test` paths and live doctor/land result.

   I posted a targeted `[chat-review]` to genome at 11:14:59Z with the line, classification, and existing step key.

3. **A newer health task was also stranded by failed dispatch during follow-through.** After the captured range, `health-warning/8dc3e96142cf71630366/triage` appeared with `dispatch=failed` and no board task. Its dispatch check for owner health exited 0; I retried `mesh-task dispatch` at 11:12:16Z. The task now appears in `tasks.journal` as `QUEUED`, owner health, dispatch sent, with the exact `[task]` post at 55878. It remains open pending health's `[taking]` and evidence.

## Live pane, repeats, and verification limits

`mesh-task audit` completed successfully during the follow-through. The live pane kept the 20-row raw tail and current unfinished count. Repeated count-only FYIs `root-mesh-devcd-catch` (141) and `witness mesh charter watch` (125) were unchanged across the pane reads and still say `task:none`; they remain visible and unpredicted. The path-watch FYI at 55776 reported imac-rozalia falling back from direct to relay, which the later health warning at 55863 explicitly records as a direct-to-relay change and routes to health. No routing, DNS, VPN, firewall, or other substrate state was edited.

This review verified task identity, owner-authored transitions, receipt presence, and SHA-256 agreement with task-state records. It did not independently repeat every sensor probe contained in the health receipts. Current remaining obligations are the genome-owned autoland reconciliation, the queued manifest-consumer step (including the library warning), the active root-sync task, and health's newly dispatched `8dc3…/triage`.
