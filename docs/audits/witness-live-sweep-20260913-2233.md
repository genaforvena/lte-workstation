# Witness live sweep — 2026-09-13 22:32–22:34 UTC

## Evidence

- `rtk mesh-dash --once witness` returned the unfiltered live stream: 1,158 task rows, 94 unfinished (1 `OPEN_UNOWNED`, 29 `QUEUED`, 58 `BLOCKED`, 6 `HELD_REJECTED`), source age 1s, and 20/61,184 raw `chat.log` lines. The pane's recurring FYI view is partial; it reports the direct-relay fallback, devcd listener-down count 142, and divergent haunt-charter count 125. Linked items remain `UNKNOWN`/`BLOCKED`.
- Read `CLAUDE.md`, `~/.mesh/tasks.journal`, and the raw last 20 lines of `~/.mesh/chat.log`; `rtk mesh-task audit` exited 0. The board tail had no duplicate witness claim. It showed a fresh `health-warning/47efd3a73e356a062d8b/triage` task routed to health after `audit-rc-124`; the current task row is dispatched to health. The router-access task remains explicitly operator-owned.
- `rtk mesh-task queue --dispatch --owner witness` returned no row. All three queued witness steps were checked and each returned exit 2: `tinyfleet-real-mesh-pilot-20260907/verify-and-report-pilot`, `crypthauntology-kids-followup-20260912/adult-study-release-independent-review`, and `task-queue-stall-tinyfleet-proof-20260912/verify-live-proof`. Their journal rows point at predecessor steps owned by haunt/another chain owner, so no claim was taken.
- Posted one current `[idle]` line at 22:33:53Z, naming the 94 unfinished rows, three refused witness candidates, and the existing health/operator ownership. `mesh-chat` returned the appended line (not suppressed).

## Later alert caught during verification

- The handoff-tail check exposed a newer `land@phaedra` `[strand]` refusal at 22:33:04Z: the same 14-file parked autostash is still blocking autoland. The prior receipt `docs/task-receipts/witness-autoland-refusal-followthrough-20260912.md` identifies the exact Phaedra object as `e31ca425f4ac26f13a17c0b3182d605946aa55cb` and leaves disposition to steward review. The local mesh-home stash list was empty, so I did not treat it as evidence about Phaedra or mutate any stash.
- No active autostash/autoland task existed. Created and dispatched `witness-autoland-repeat-20260913/reconcile-current-repeat` to genome from `docs/audits/witness-autoland-repeat-20260913.plan.tsv`; `mesh-task check dispatch ... genome` returned 0. `mesh-task status` reports the step OPEN, owner genome. The task asks for fresh read-only Phaedra evidence and steward routing; it explicitly forbids applying, dropping, popping, rebasing, or resetting the stash.
- The structured ledger and task view initially disagreed: `mesh-task audit` saw the new row, while `tasks.journal` and the pane were still one task behind. `mesh-task rebuild` only rebuilt per-chain caches, so I ran the actual `mesh-task-journal` materializer. The row then appeared at `QUEUED genome ... dispatch=sent`; a fresh `mesh-dash --once witness` showed 1,159 total / 95 unfinished, source age 6s, and the new row in the visible top 20. A fresh `mesh-task audit` also includes it (exit 0).
- Genome posted owner-authored `[taking]` at 22:39:48Z; the ledger advanced to ACTIVE with lease through 23:09:47Z. The first `mesh-task-journal` refresh skipped because another replay held its lock; retry published successfully. The final materialized row is `RUNNING genome ... lease=2026-09-13T23:09:47Z`, and the pane agrees at 1,159 total / 95 unfinished / 2 running, source age 3s.
- The FYI event count advanced from 9471 to 9472. The new 22:40:08Z `udev-stream` line reports 2 events / 1 unsigned and unattributed, listener up, named hwmon change. No active `udev`/`hwmon`/device-churn task exists; this was recorded as a new observation, with no duplicate task created.
- Genome's next ledger event completed health's `audit-rc-124` triage and created `phaedra-autostash-steward-disposition-20260913/review-parked-object`, owner `steward`, dispatch sent. `mesh-task check dispatch ... steward` returned 0; `mesh-task status` confirms the row OPEN. The current audit shows that steward row QUEUED and the genome reconciliation row RUNNING. No steward `[taking]` is visible yet; the task remains open pending the actual review.
- Genome's progress artifact `docs/task-receipts/witness-autoland-repeat-20260913.md` confirms the exact Phaedra object and creates the steward review row. `mind-control` then reported that row HELD for manual human action (not mind-dispatchable); this is consistent with its human owner `steward` and the absence of a steward `[taking]`, so it remains QUEUED for manual pickup. I tried to type-block it as witness, but `mesh-task` refused with `exact owner required: task owner=steward actor=witness`; no state changed and I did not impersonate the owner.
- Final pane refresh at 22:44:55Z showed 1,160 total / 95 unfinished / 947 done; `RUNNING=1`, `QUEUED=29`, `BLOCKED=58`, `OPEN_UNOWNED=1`, `HELD_REJECTED=6`; source age 45s and 20/61,214 raw tail lines. The new genome row is RUNNING; the human steward row remains QUEUED. `mesh-task audit` exited 0 and independently returned those two rows plus the completed health triage. `mesh-wake-expect witness` was installed and `--show` verified four bounded patterns for source-age, FYI age with event count pinned at 9472, pane-live timestamp, and Note3 battery line; TTL 300s.
- The initial raw-tail snapshot ended at 22:32:14Z, before this 22:33:04Z alert. The `[idle]` at 22:33:53Z therefore failed to account for an event already on the board; the post-handoff tail check exposed it. No second idle was posted. The genome-owned corrective task is now the active outcome of the sweep.

## Next check

On the next wake, run `mesh-dash --once witness`, reread `~/.mesh/tasks.journal` and `~/.mesh/chat.log`, run `mesh-task audit`, and check the genome reconciliation for progress/artifact plus the steward review task for owner-authored `[taking]` and its disposition. Harmless prediction is limited to source-age, FYI age/source-cutoff with event count pinned at 9472, pane-live timestamp, and the recurring Note3 battery line; changes to task ownership/state, board claims, incident counts, or health warnings must wake the pane.
