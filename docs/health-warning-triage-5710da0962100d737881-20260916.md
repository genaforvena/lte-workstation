# Health-warning triage: `health-warning/5710da0962100d737881`

Task: `health-warning/5710da0962100d737881/triage`

## Source warning

The task was created from the 2026-09-15T20:03:46Z health roll-call:
`route:no | PROPOSE none | CHANGED fail2ban repeat triage closed; remote jail read PASS | GAP comprehensive doctor still running; last completed 1 FAIL/33 WARN`.

## Live checks

- `mesh-task status health-warning/5710da0962100d737881` showed one active step,
  owned by `health`, with lease through 2026-09-16T03:50:37Z.
- `mesh-health` completed with `rc=0`: this node, GL-MT3000, Redmi 10,
  imac-rozalia, and phaedra were reachable; ilya, imozerov-Default-string,
  imozerov-IdeaPad-3-15IIL05, and rip were offline.
- `mesh-load-audit --json` completed with `rc=0` at 03:22:16Z:
  `ORGAN-LOAD`, `GPU-IDLE`, load1 `18.91/16`, top process `python3[organ]`,
  no burn (`burn_n=0`). This explains the contemporaneous probe warning as
  known workload pressure, not evidence of a substrate failure.
- A fresh `mesh-fleet-health` completed with `rc=0` at 03:22:28Z, but still
  reported `LOCAL LOAD HIGH` and rendered non-answers as `UNKNOWN(load)`.
- A bounded `timeout 90s mesh-doctor --quiet` did not complete. Its captured
  output contained only `WARN mic DEFAULT device broken/busy`; a separate
  pre-existing `mesh-doctor` process was already running for about an hour.
  The exact child processes started by this triage were terminated after the
  timeout; no substrate or repository configuration was changed.

## Decision

The fail2ban and remote-jail portions are already resolved according to the
source warning. The comprehensive-doctor gap remains unresolved and is a
known blind spot, not a claimed green result. Retry after the pre-existing
doctor run exits (or after the doctor runner is repaired), using a bounded run
and preserving its complete output as evidence. The current load/probe warning
is attributable to known organ workload with no sustained burn observed.

Delegation: a read-only `health-load-audit` worker independently inspected the
warning path and wrote `/tmp/health-load-audit-report.md` (SHA-256
`e09b786ab9cb42ce01e54e12ea1a1218df0a188b959bd6f2f001ae377f77dd37e`). I
personally inspected that report; its threshold and wiring findings agree with
the direct `mesh-load-audit` and `mesh-fleet-health` observations above.
