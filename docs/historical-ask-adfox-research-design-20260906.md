# Historical ask 20260821T004729Z — AdFox research design

Source: `~/.mesh/voice-in.log`, `2026-08-21T00:47:29Z`.

This ask requests a focused investigation of AdFox auction data, duplicate/prior-art
checking, and wider collection. The resulting artifact is a research design, not a
claim that the investigation was performed.

Record each observation with URL, retrieval timestamp, query/profile conditions,
field names, and a raw response hash. Separate observed auction fields from
inferences about bidders or targeting. First run a prior-art search over the
existing Adint notes and cite collisions; then collect a bounded sample across
multiple placements and dates. Publish a table of observed fields, coverage,
redactions, and uncertainty, with no identity claims unsupported by the raw data.
