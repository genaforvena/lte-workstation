# Prioritized tiny-fleet roadmap

1. **P0 — restore claim validity:** remove or explain the six duplicate blobs in snapshot B; rerun
   leakage and split audits. Until then, no clean study estimate is allowed.
2. **P0 — restore corpus scale:** resolve immutable commits and license evidence for the declared
   pilot repositories, then reach the preregistered usable-repository target before pooling.
3. **P1 — complete measured ladder:** add behavioral/generative held-out rows, shuffled and base
   controls, prompt-family separation, abstention, and calibration metrics.
4. **P1 — unblock genuine update arm:** provide pinned `torch`, `transformers`, and `peft` plus
   accelerator/resource evidence; retain adapter hash, config, logs, and before/after outputs.
5. **P1 — strengthen uncertainty:** cluster bootstrap by repository/file/blob as applicable and
   publish per-repository and per-stratum estimates with missingness.
6. **P2 — temporal/generalization extension:** add three snapshots where eligible and lock one whole
   repository per stratum as an untouched holdout.
7. **P2 — release gate:** independent witness reruns hashes, disjointness, two controls, one
   mutation, and headline-table recomputation before any architectural interpretation.
