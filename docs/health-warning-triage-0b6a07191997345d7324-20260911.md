# Health warning triage: `mesh-home:wake` mind-holding

Date: 2026-09-11
Task: `health-warning/0b6a07191997345d7324/triage`

## Finding

The warning at `2026-09-11T14:48:00Z` is a historical, non-actionable
`UNATTRIBUTABLE` composer hold. The held text was `› /clear`; the keepalive arm
correctly refused to inject keys because `mesh-tell` WAL had no matching
delivery attribution. This is the safety behavior specified by
`strand_sweep()` in `scripts/mesh-channel-keepalive` (the `STRAND-$attr` arm
logs and posts `[mind-holding]`, then leaves the composer unchanged).

## Evidence

- `/home/mesh-home/.mesh/chat.log:46831` records the exact warning and text.
- `/home/mesh-home/.mesh/.strand-held-wake` has mtime
  `2026-09-11 14:48:00.852732663 UTC` and is zero bytes, matching the
  once-per-repost hold marker rather than a queued recovery action.
- `/home/mesh-home/.mesh/tell-wal.log` has no `wake` intent/sent pair near the
  event; nearby rows route to other windows. Therefore attribution remains
  unknown, not evidence that the operator or harness submitted the text.
- The current pane stream reports the warning as historical; no live
  `wake` strand action is indicated.

## Disposition

No substrate or pane mutation is safe or warranted. Keep this as a known
blindness: the detector can report an unchanged composer and absence of WAL
attribution, but cannot determine whether an operator's hand was still
composing. The existing behavior—leave it alone and emit `[mind-holding]`—is
correct.

Verification: `mesh-dash --once check` completed with exit 0; exact task
eligibility check completed with exit 0; source and local evidence were read
directly.
