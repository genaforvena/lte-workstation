# Fixture input

This tiny offline input is a protocol fixture, not a corpus claim. It contains
source-like text, toy train/heldout rows, exact-copy leakage, an abstention
case, an empty file, a renamed duplicate, and a generated path so the
extractor records inclusion/exclusion decisions deterministically.
