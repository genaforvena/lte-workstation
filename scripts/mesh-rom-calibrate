#!/usr/bin/env bash
# mesh-rom-calibrate — deploy shim for the ROM-vs-shell calibration ledger
# (fixed-point lane; operator 2026-07-23: the ROM is an ORACLE — run both
# implementations on identical inputs, spend the DISAGREEMENT signal).
# Real tool lives with its ROMs at scripts/uxn/mesh-rom-calibrate.
# Daily, after mesh-lease-audit's 05:41 (same input resolver, fresh rows).
# reflex-cadence: 7 6 * * *
set -eu
REPO="${MESH_REPO:-$HOME/lte-workstation}"
TARGET="$REPO/scripts/uxn/mesh-rom-calibrate"
[ -x "$TARGET" ] || { echo "mesh-rom-calibrate: n/a — no genome checkout at $TARGET (set MESH_REPO)" >&2; exit 2; }
exec "$TARGET" "$@"
