#!/usr/bin/env bash
# test-packet-gate — thin wrapper over `mesh-packet-gate --test` (test-* canon; on-demand, not cron).
# The real RED-first→GREEN proof (manifest · rc0/1/2 truth table · sha256 pin · a REAL packet DROPPED
# through a REAL NFQUEUE in a netns · pps · Note3 armhf byte-identity) lives in mesh-packet-gate.
exec "$(cd "$(dirname "$0")" && pwd)/mesh-packet-gate" --test
