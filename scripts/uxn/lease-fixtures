# lease-fixtures — reflex watchdog leases (name  cadence_s  lease_s)
# The invariant (mesh-reflex-health eff_maxage @auto): lease MUST be >= 2*cadence,
# else the reflex reads STALE every cycle while perfectly healthy. Real bug: lease==cadence.
ambient-clock   600   1200
body-motion     900   1800
reflex-health   3600  7200
