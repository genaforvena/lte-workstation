#!/usr/bin/env bash
# orphan-ok: on-demand NVIDIA video-encoder utilization read; no consumer has a sampling cadence yet.
# mesh-gpu-encoder — live activity percentage from each NVIDIA GPU's NVENC engine.
# This reports encoder-engine utilization, not general GPU utilization or video quality.
# Unreadable/unsupported data is UNKNOWN (exit 2); N/A is never turned into zero.
#
#   mesh-gpu-encoder          one live reading
#   mesh-gpu-encoder --json   machine-readable reading
#   mesh-gpu-encoder --test   fixture parsing + a real NVIDIA driver read
set -uo pipefail
export PATH="${HOME:-}/.local/bin:$PATH"

SMI="${MESH_GPU_ENCODER_SMI:-nvidia-smi}"
ROWS=()

read_rows() {
  local raw line index percent extra
  raw="$("$SMI" --query-gpu=index,utilization.encoder --format=csv,noheader,nounits 2>/dev/null)" || return 2
  [ -n "$raw" ] || return 2
  ROWS=()
  while IFS= read -r line; do
    [ -n "${line//[[:space:]]/}" ] || continue
    IFS=',' read -r index percent extra <<<"$line"
    index="$(printf '%s' "${index:-}" | tr -d '[:space:]')"
    percent="$(printf '%s' "${percent:-}" | tr -d '[:space:]')"
    extra="$(printf '%s' "${extra:-}" | tr -d '[:space:]')"
    case "$index" in ''|*[!0-9]*) return 2;; esac
    case "$percent" in ''|*[!0-9]*) return 2;; esac
    [ -z "$extra" ] && [ "$percent" -le 100 ] || return 2
    ROWS+=("$index $percent")
  done <<<"$raw"
  [ "${#ROWS[@]}" -gt 0 ] || return 2
}

render_json() {
  local row index percent first=1
  printf '{"source":"nvidia-smi","engines":['
  for row in "${ROWS[@]}"; do
    read -r index percent <<<"$row"
    [ "$first" = 1 ] || printf ','
    first=0
    printf '{"gpu_index":%s,"encoder_utilization_percent":%s}' "$index" "$percent"
  done
  printf ']}\n'
}

if [ "${1:-}" = --test ]; then
  td="$(mktemp -d "${TMPDIR:-/tmp}/mesh-gpu-encoder.XXXXXX")" || exit 1
  trap 'rm -rf "$td"' EXIT
  cat >"$td/smi-ok" <<'EOF'
#!/usr/bin/env bash
printf '0, 37\n1, 4\n'
EOF
  cat >"$td/smi-na" <<'EOF'
#!/usr/bin/env bash
printf '0, N/A\n'
EOF
  chmod +x "$td/smi-ok" "$td/smi-na"
  fixture="$(MESH_GPU_ENCODER_SMI="$td/smi-ok" "$0" --json 2>/dev/null)" || {
    echo 'mesh-gpu-encoder --test: FAIL (fixture parse)' >&2; exit 1;
  }
  [[ "$fixture" == *'"gpu_index":0,"encoder_utilization_percent":37'* &&
     "$fixture" == *'"gpu_index":1,"encoder_utilization_percent":4'* ]] || {
    echo "mesh-gpu-encoder --test: FAIL (fixture output: $fixture)" >&2; exit 1;
  }
  MESH_GPU_ENCODER_SMI="$td/smi-na" "$0" --json >/dev/null 2>&1
  rc=$?
  [ "$rc" -eq 2 ] || { echo "mesh-gpu-encoder --test: FAIL (N/A must exit 2, got $rc)" >&2; exit 1; }

  live="$("$0" --json 2>/dev/null)"
  rc=$?
  if [ "$rc" -eq 2 ]; then
    echo 'mesh-gpu-encoder --test: n/a — fixtures passed; live encoder data unavailable (exit 2)' >&2
    exit 2
  fi
  [ "$rc" -eq 0 ] && [[ "$live" == *'"encoder_utilization_percent":'* ]] || {
    echo "mesh-gpu-encoder --test: FAIL (live driver read rc=$rc output=$live)" >&2; exit 1;
  }
  echo "mesh-gpu-encoder --test: PASS (fixtures, N/A exit-2, live driver read: $live)"
  exit 0
fi

mode=text
case "${1:-}" in
  '') ;;
  --json) mode=json;;
  *) echo 'usage: mesh-gpu-encoder [--json|--test]' >&2; exit 1;;
esac

read_rows || {
  echo 'gpu-encoder: UNREACHABLE — NVIDIA driver has no numeric encoder utilization reading' >&2
  exit 2
}
if [ "$mode" = json ]; then
  render_json
else
  for row in "${ROWS[@]}"; do
    read -r index percent <<<"$row"
    printf '[gpu-encoder] gpu_index=%s encoder_utilization_percent=%s (NVENC engine)\n' "$index" "$percent"
  done
fi
