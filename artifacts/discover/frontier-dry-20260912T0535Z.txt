discover frontier sweep — mesh-home — 2026-09-12T05:35Z

Live cockpit: `mesh-dash --once discover` at 2026-09-12T05:33:59Z.

Reachable adjacent surfaces checked:
- AMD CPU hwmon (`name=k10temp`): Tctl=69.000C (`temp1_input=69000`),
  Tccd1=67.500C (`temp3_input=67500`). Prior-art verdict for
  `k10temp temp1_input Tctl temp3_input Tccd1 AMD CPU thermal reading`:
  NEAR-MATCH; existing catalog is
  `~/.mesh/knowledge/capability-hw-health-amd-thermal-blind-20260714.md` and
  `scripts/mesh-cpu-thermal-spread`. Not a fresh sense.
- CPU package power: `mesh-package-power` returned a real 2.0s sample,
  NORMAL, package=89.69W, core=9.12W (per-core domain). The raw
  `/sys/class/powercap/.../energy_uj` read is root-only, but the established
  consumer's `sudo -n` path succeeds. Existing catalog:
  `~/.mesh/knowledge/capability-cpu-energy-rapl-root-blocked-amd-migration-regression-20260718.md`
  (superseded 2026-08-30 with the live-consumer correction) and
  `scripts/mesh-package-power`. Prior-art verdict for
  `intel-rapl:0 package-0 AMD Ryzen energy_uj powercap`: NEAR-MATCH. No new
  consumer or new capability established by this reading.

Decision: FRONTIER DRY for this sweep. The live adjacent CPU thermal and power
readings both land on already-catalogued paths; no fresh knowledge or duplicate
steward task is warranted. Next useful frontier is a genuinely new reachable
node or signal.
