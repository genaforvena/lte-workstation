discover frontier sweep — mesh-home — 2026-09-11T00:46Z

Candidate: ext4 superblock/filesystem fault counters under /sys/fs/ext4/*.
Intended consumer predicate: every real mounted ext4 directory must expose readable
numeric errors_count, warning_count, and msg_count values.

Live price (three one-second samples):
- dm-0: 3/3 samples readable and numeric for all three counters (9/9 fields).
- nvme0n1p2: 3/3 samples readable and numeric for all three counters (9/9 fields).
- features/ was excluded because it is the ext4 features metadata directory, not a
  filesystem counter instance.
- acceptance: 6/6 filesystem samples passed = 100% (18/18 fields).
- observed values were stable: dm-0 errors=0 warning=0 msg=2; nvme0n1p2
  errors=0 warning=0 msg=1.

Prior-art gate, before any knowledge filing:
  mesh-prior-art '/sys/fs/ext4 errors_count warning_count msg_count ext4 filesystem fault counters'
  verdict: NEAR-MATCH.
  Existing catalog: capability-ext4-superblock-write-accumulators-...-20260817.md
  and scripts/mesh-fs-integrity already cover errors_count; the board also records
  the ext4 errors_count sweep as an already-taken axis.

Decision: reject as a fresh find. The reach is real and priced, but it is already
catalogued; no new knowledge file or steward task is created. Revisit only for a
distinct uncovered ext4 field with a separate consumer question.
